# MVP implementation plan

1. **Safe foundation (implemented here):** exact host scope, passive portal/application detection, replacement-aware iframe registry, responsive shell, error/session states, original-UI escape hatch, synthetic fixtures, network-denied tests, and an allowlisted academic-process catalog that visibly blocks consequential routes.
2. **First functional read (implemented):** navigate to either observed grade-query entry through exact allowlisted menu labels after an explicit user action; share the fingerprinted grade application; select a visible program; optionally include withdrawn courses; select one academic period; and display only that period's normalized course rows responsively in memory.
3. **Next safe milestone:** add fixture-driven parsing for the already-rendered period statistics, then study current enrollment and schedule. Registration-window values remain outside automated discovery.
4. **Later read-only milestone:** add explicit academic-offer search delegation only after sanitized result/error fixtures exist and a request governor is implemented. No search-on-type or automatic retry.
5. **Local planning:** build a registration planner that never contacts SAP.
6. **Transactional work:** remains out of scope until a separate, explicitly authorized design and validation effort establishes confirmation boundaries and complete sanitized state-machine fixtures.
